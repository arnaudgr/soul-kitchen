<div class="breadcumb-area bg-img bg-overlay" style="background-image: url(<%= asset_path 'recette6.jpg'%>);">
    <div class="container h-100">
        <div class="row h-100 align-items-center">
            <div class="col-12">
                <div class="breadcumb-text text-center">
                    <h2>Laisse un commentaire & un avis à la recette ! </h2>
                </div>
            </div>
        </div>
    </div>
</div>




<div class="container h-100">
    <div class="row h-100 align-items-center">
        <div class="col-12">
            <div class="breadcumb-text text-center">

                <%= form_for([@recipe, review]) do |f| %>
                  <% if review.errors.any? %>
                    <div id="error_explanation">
                      <h2><%= pluralize(review.errors.count, "error") %> prohibited this review from being saved:</h2>

                      <ul>
                      <% review.errors.full_messages.each do |message| %>
                        <li><%= message %></li>
                      <% end %>
                      </ul>
                    </div>
                  <% end %>

                  <div class="field">
                    <%= f.label :comment, "Votre commentaire" %><br>
                    <%= f.text_area :comment, placeholder:"Votre Message", :cols=>"30", :rows =>"10", class:'form-control' %><br>
                  </div>
                    <p>Note </p>
                  <div class="field" style="margin-left: 2%;"  >
                    <div id="star-rating"  ></div>
                  </div>

                  <div class="actions">
                    <%= f.submit "Envoyez", class:"btn delicious-btn mt-30" %>
                  </div>
                <% end %>

            </div>
        </div>
    </div>
</div>


<script>
  $('#star-rating').raty({
    path: '/assets/',
    scoreName: 'review[rating]'
  });
</script>